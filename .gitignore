# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.ruff_cache/

# Virtual environments
.venv/
venv/
ENV/
env/

# Docker
.dockerignore

# IDEs
.vscode/
.idea/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.next/
frontend/out/
frontend/build/

# Logs
*.log

# Cloud Storage (Hugging Face Hub)
# Training data and model weights live in HF Hub — do NOT commit locally
backend/data/
backend/routes/ai/models/*.pth

# Runtime uploads (user X-ray images and generated Grad-CAM heatmaps)
backend/uploads/

# NOTE: backend/.env is intentionally NOT ignored — it is committed to the repo
# so the app works out of the box on a fresh clone.
# Rotate any secrets after sharing the repo publicly.
